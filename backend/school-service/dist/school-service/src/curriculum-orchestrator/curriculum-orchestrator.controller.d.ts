import { CurriculumPlannerService } from './services/planner.service';
import { CurriculumTrackingService } from './services/tracking.service';
import { CurriculumReschedulerService } from './services/rescheduler.service';
import { LogTeachingDto } from './dto/orchestrator.dto';
import { AcademicPlanningService } from '../academic-planning/academic-planning.service';
export declare class CurriculumOrchestratorController {
    private readonly plannerService;
    private readonly trackingService;
    private readonly reschedulerService;
    private readonly academicPlanningService;
    constructor(plannerService: CurriculumPlannerService, trackingService: CurriculumTrackingService, reschedulerService: CurriculumReschedulerService, academicPlanningService: AcademicPlanningService);
    generatePlan(): Promise<void>;
    getDashboardSummary(req: any, classId: string, academicYearId: string): Promise<({
        subject_name: any;
        plan_id: string;
        subject_id: string;
        completion_percentage: number;
        total_topics: number;
        completed_topics: number;
        delayed_topics: number;
        delay_days: number;
        remaining_topics: number;
        remaining_days: number;
        risk_status: string;
        next_scheduled_date: string | null;
    } | null)[]>;
    getPlan(req: any, id: string): Promise<{
        items: {
            status: string;
            id: string;
            plan_id: string;
            topic_id: string;
            planned_date: string;
            planned_sessions: number;
            plan: import("@lumiqos/shared/index").CurriculumPlan;
            syllabus: import("@lumiqos/shared/index").Syllabus;
            created_at: Date;
            updated_at: Date;
        }[];
        progress: number;
        id: string;
        school_id: string;
        academic_year_id: string;
        class_id: string;
        subject_id: string;
        total_topics: number;
        total_estimated_hours: number;
        planned_start_date: string;
        planned_end_date: string;
        status: string;
        school: import("@lumiqos/shared/index").School;
        academicYear: import("@lumiqos/shared/index").AcademicYear;
        class: import("@lumiqos/shared/index").Class;
        subject: import("@lumiqos/shared/index").Subject;
        created_at: Date;
        updated_at: Date;
    }>;
    logTeaching(req: any, dto: LogTeachingDto): Promise<import("@lumiqos/shared/index").TeachingLog>;
    recalculate(): Promise<void>;
}
