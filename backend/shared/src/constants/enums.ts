export enum AcademicYearStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    UPCOMING = 'upcoming',
    COMPLETED = 'completed'
}

export enum EnrollmentStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    WITHDRAWN = 'withdrawn',
    GRADUATED = 'graduated',
    SUSPENDED = 'suspended',
    PROMOTED = 'promoted',
    COMPLETED = 'completed'
}

export enum StudentStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    ARCHIVED = 'archived',
    PENDING = 'pending'
}

export enum StudentMarksStatus {
    SUBMITTED = 'submitted',
    PUBLISHED = 'published',
    PENDING = 'pending',
    MISSING = 'missing',
    ENTERED = 'entered'
}

export enum HomeworkStatus {
    PENDING = 'pending',
    SUBMITTED = 'submitted',
    GRADED = 'graded',
    LATE = 'late'
}

export enum InvoiceStatus {
    PAID = 'paid',
    PARTIAL = 'partial',
    PENDING = 'pending',
    OVERDUE = 'overdue',
    CANCELLED = 'cancelled'
}

export enum ExamStatus {
    DRAFT = 'draft',
    SCHEDULED = 'scheduled',
    ONGOING = 'ongoing',
    COMPLETED = 'completed',
    PUBLISHED = 'published'
}

export enum AttendanceStatus {
    PRESENT = 'present',
    ABSENT = 'absent',
    LATE = 'late',
    EXCUSED = 'excused'
}
